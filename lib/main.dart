import 'package:flutter/material.dart';

void main(List<String> args) {
  runApp(
    Center(
      child: Text(
        "Olá Flutter",
        textDirection: TextDirection.ltr,
      ),
    )
  );
}